---
description: Analyze this repository and create or improve its AGENTS.md
---

Analyze this codebase and create an AGENTS.md file (or improve the existing one) that gives a coding agent everything it needs to work here effectively.

Steps:
1. If an AGENTS.md (or CLAUDE.md / .agents/AGENTS.md) already exists, read it first — improve it rather than replacing it, and preserve rules that encode real learned constraints.
2. Explore the repository: README, package manifests, build/test configuration, directory layout, and a few representative source files.
3. Write a concise AGENTS.md (aim for under 40 lines) covering only what an agent cannot derive by reading code on demand:
   - Build, test, lint, and typecheck commands (including how to run a single test).
   - Architecture in a few sentences: major components and how they connect.
   - Project-specific conventions that differ from language defaults (imports, formatting, error handling, naming).
   - Any workflow rules (branch naming, commit style, files that must not be edited).
4. Do NOT include generic advice ("write clean code"), file trees that will go stale, or anything obvious from a glance at the repo.

Show me the proposed AGENTS.md content before writing the file.
