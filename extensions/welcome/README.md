# Welcome

Startup banner for interactive parent sessions: shows a short bundle map /
flow summary plus current git context (branch, status) and a spend summary
(reusing `usage-extension`'s `collectUsageData`).

## User surface

Automatic on interactive session start; no tools or commands.

## Origin

Bundle-local.
